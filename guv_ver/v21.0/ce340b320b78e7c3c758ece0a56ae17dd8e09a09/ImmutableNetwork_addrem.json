{
    "addition": {
        "74": "    // ImmutableMap.Builder maintains the order of the elements as inserted, so the map will\n",
        "75": "    // have whatever ordering the graph's nodes do, so ImmutableSortedMap is unnecessary even if the\n",
        "76": "    // input nodes are sorted.\n",
        "85": "    // ImmutableMap.Builder maintains the order of the elements as inserted, so the map will\n",
        "86": "    // have whatever ordering the graph's edges do, so ImmutableSortedMap is unnecessary even if the\n",
        "87": "    // input edges are sorted.\n"
    },
    "removed": {}
}