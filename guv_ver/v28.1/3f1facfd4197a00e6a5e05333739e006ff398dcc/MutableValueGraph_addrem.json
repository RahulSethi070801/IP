{
    "addition": {
        "45": "   * Adds an edge connecting {@code nodeU} to {@code nodeV} if one is not already present, and sets\n",
        "46": "   * a value for that edge to {@code value} (overwriting the existing value, if any).\n"
    },
    "removed": {
        "45": "   * Adds an edge connecting {@code nodeU} to {@code nodeV} if one is not already present, and\n",
        "46": "   * sets a value for that edge to {@code value} (overwriting the existing value, if any).\n"
    }
}